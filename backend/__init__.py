"""
backend/__init__.py
────────────────────
Marks the `backend` directory as a Python package, enabling imports like:
    from backend.config import settings
    from backend.models.user import User
"""
