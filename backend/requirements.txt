# ─────────────────────────────────────────────────────────────────────────────
# BIM Repository Auth Backend — Python Dependencies
# Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0          # ASGI server with WebSocket & HTTP/2 support

# Database (async PostgreSQL)
sqlalchemy==2.0.30                 # ORM with async support
greenlet==3.4.0                    # Required by SQLAlchemy async engine (not auto-installed on all platforms)
asyncpg==0.29.0                    # Async PostgreSQL driver (fastest available)
alembic==1.13.1                    # Database migration tool

# Password hashing — Argon2 (winner of the Password Hashing Competition 2015)
argon2-cffi==23.1.0                # Python bindings for libargon2

# JWT authentication
python-jose[cryptography]==3.3.0   # JWT encode/decode with RS256/HS256 support

# 2FA / TOTP
pyotp==2.9.0                       # RFC 6238 TOTP implementation
qrcode[pil]==7.4.2                 # QR Code generation; [pil] adds image support
Pillow==10.3.0                     # Image processing (required by qrcode[pil])

# Configuration & environment variables
python-dotenv==1.0.1               # Load .env files into os.environ
pydantic==2.7.1                    # Data validation (used by FastAPI)
pydantic-settings==2.2.1           # Settings management from env vars

# HTTP utilities / security
python-multipart==0.0.9            # Required for form data / file uploads in FastAPI
passlib==1.7.4                     # Compatibility shim used internally by python-jose

# Async file I/O — used for writing uploaded BIM files to disk
aiofiles==23.2.1
