# ─────────────────────────────────────────────────────────────────────────────
# BIM Repository Auth Backend — Python Dependencies
# Install:        pip install -r requirements.txt
# Regenerate hashes (supply chain security):
#   pip-compile --generate-hashes requirements.in -o requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database (async PostgreSQL)
sqlalchemy==2.0.30
greenlet==3.4.0                    # Required by SQLAlchemy async engine on some platforms
asyncpg==0.29.0
alembic==1.13.1

# Password hashing — Argon2id (Password Hashing Competition winner)
argon2-cffi==23.1.0

# Cryptography — AES-256-GCM (crypto_service) + JWT (python-jose dependency)
cryptography==42.0.8
python-jose[cryptography]==3.3.0

# 2FA / TOTP
pyotp==2.9.0
qrcode[pil]==7.4.2
Pillow==10.3.0

# Configuration & environment variables
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# HTTP utilities / security
python-multipart==0.0.9            # Required for form data / file uploads in FastAPI
passlib==1.7.4                     # Compatibility shim used internally by python-jose

# Rate limiting — brute-force protection on auth endpoints
slowapi==0.1.9

# HTTP client — Supabase Storage communication
httpx==0.27.0
